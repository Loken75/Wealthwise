package com.wealthwise.domain.shared;

/**
 * Devises supportées par l'application.
 *
 * C'est un enum Java — un type qui ne peut prendre que des valeurs prédéfinies.
 * On commence avec peu de devises, on en ajoutera si besoin.
 */
public enum Currency {
    EUR,
    USD,
    GBP
}
